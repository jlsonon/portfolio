import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
    images: {
        remotePatterns: [
            // Add external image hosts here only if you are actively using them
        ],
    },
};

export default nextConfig;
